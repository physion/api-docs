# Sample Labels
## Get All Sample Labels

**Summary:** Get available sample labels
<%= partial "./partial_layouts/block_quotes.html.erb" %>

```ruby
require "uri"
require "net/http"

url = URI("{api_endpoint}/api/v3/project_templates/{project_template_id}/sample_labels")

http = Net::HTTP.new(url.host, url.port)
request = Net::HTTP::Get.new(url)
request["Authorization"] = "Bearer {token}"

response = http.request(request)
puts response.read_body
```

```json-doc
```

```json
{
    "sampleLabels": {
        "labels": [
            "DNA"
        ]
    }
}
```

### HTTP Request
`GET /v3/project_templates/{project_template_id}/sample_labels`

**Parameters**

| Name | Located in | Description | Required | Type |
| ---- | ---------- | ----------- | -------- | ---- |
| project_template_id | path | Associated project_template group | Yes | string |

**Responses**

| Code | Description |
| ---- | ----------- |
| 200 | Sample Labels |

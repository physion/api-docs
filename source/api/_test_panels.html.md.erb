# Test Panels
## Get All Test Panels

**Summary:** Get available Test Panels
<%= partial "./partial_layouts/block_quotes.html.erb" %>

```ruby
require "uri"
require "net/http"

url = URI("<%= config[:api_env_endpoint] %>/api/v3/project_templates/{project_template_id}/test_panels")

http = Net::HTTP.new(url.host, url.port)
request = Net::HTTP::Get.new(url)
request["Authorization"] = "Bearer {token}"

response = http.request(request)
puts response.read_body

```

```json-doc
```

```json
{
    "testPanels": [
        {
            "panelName": "Genome Exploration  PGx Comp",
            "panelCode": "COR_COMP"
        },
        {
            "panelName": "GENERIC_COMP",
            "panelCode": "GENERIC_COMP"
        },
        {
            "panelName": "Intermountain Healthcare Comp",
            "panelCode": "Intermountain_Healthcare_Comp"
        },
        {
            "panelName": "Clay's Test Panel",
            "panelCode": "CTP"
        }
    ]
}
```

### HTTP Request
<span class="method get">GET</span>
` /v3/project_templates/{project_template_id}/test_panels`

**Parameters**

| Name | Located in | Description | Required | Type |
| ---- | ---------- | ----------- | -------- | ---- |
| `project_template_id` | path | Associated project_template group | Yes | `string` |

**Responses**

| Code | Description |
| ---- | ----------- |
| 200 | Test Panels |
